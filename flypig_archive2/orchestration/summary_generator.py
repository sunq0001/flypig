"""短语摘要生成

为什么做：每轮对话结束后需要生成 ≤50 字的短语摘要作为会话列表标题，方便用户快速找到历史对话。
实现方法：调用 LLM 对当前轮次的对话内容生成一句话摘要。~40 行。
实现效果：历史会话列表显示标题，用户根据标题找到对应的历史对话。
技术栈：根据本轮交互生成 ≤50 字摘要, ~40 行

层&依赖：orchestration 层，依赖 IModel（调用 LLM 生成摘要）
细节见文档：docs/docs_refactor/backend-modules.md → §SummaryGenerator
"""
