"""DomainService — 领域服务标记基类

为什么做：某些业务逻辑不属于任何一个具体的聚合根（如定价计算、模型路由），
又或者需要协调多个聚合根完成一个操作，此时需要 DomainService。

实现方法：ABC 标记基类。约束：无状态，所有入参通过方法参数传递，
只依赖 Repository 接口和领域对象，不依赖基础设施。

层&依赖：shared.kernel 层，零依赖
"""

from abc import ABC


class DomainService(ABC):
    """领域服务标记基类 — 无状态，处理跨聚合的业务逻辑

    什么时候用 DomainService：
        - 业务逻辑不属于任何一个具体的聚合根
        - 需要协调多个聚合根完成一个操作
        - 需要调用外部服务的领域逻辑（如价格计算、策略判断）

    约束：
        - 必须无状态（不能有实例变量）
        - 所有入参通过方法参数传递
        - 只依赖 Repository 接口和领域对象，不依赖基础设施
    """
    pass
